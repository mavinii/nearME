import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
    container:{
        maxHeight: 200,
        position: 'absolute',
        zIndex: 1,
        top: 64,
    },
    content:{
        gap: 8,
        paddingHorizontal: 16,
    },
});